#!/usr/bin/env bash
# XXX tmux doesn't save the buffer to the system clipboard in Alacritty. This
# is a temporary work-around.

_my_tmux_save_buffer() {
  [[ "${TERM}" =~ ^screen ]] || return 0
  tmux save-buffer - | pbcopy
}

precmd_functions+=(_my_tmux_save_buffer)
