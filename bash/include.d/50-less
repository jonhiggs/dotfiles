export LESS="-R"

_lessc() {
  /usr/local/Cellar/source-highlight/3.1.8_9/bin/src-hilite-lesspipe.sh $1 | less
}

alias lessc="_lessc"

for cmd in /usr/bin/lesspipe /usr/local/bin/lesspipe.sh; do
  [[ -x ${cmd} ]] || continue
  eval "$(SHELL=/bin/sh ${cmd})"
done

# vim: ft=sh
